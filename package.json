{
  "name": "cypress",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "amsin": "cypress run  --env ENV=amsin  --browser chrome",
    "beta": "cypress run  --env ENV=beta  --browser chrome",
    "ams": "cypress run  --env ENV=ams  --browser chrome"
  },
  "keywords": [],
  "author": "Eraganaboina Vinod Kumar",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^10.3.0",
    "cypress-mochawesome-reporter": "^3.3.0",
    "cypress-wait-until": "^1.7.2",
    "cypress-xpath": "^2.0.1",
    "node-xlsx": "^0.21.0"
  },
  "dependencies": {
    "jsonpath": "^1.1.1"
  }
}
