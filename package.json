{
  "name": "cypress",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cy:run:amsin": "cypress run  --env ENV=amsin",
    "cy:run:beta": "cypress run  --env ENV=beta",
    "cy:run:ams": "cypress run  --env ENV=ams"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^10.3.0",
    "cypress-xpath": "^2.0.1",
    "node-xlsx": "^0.21.0"
  },
  "dependencies": {
    "jsonpath": "^1.1.1"
  }
}
